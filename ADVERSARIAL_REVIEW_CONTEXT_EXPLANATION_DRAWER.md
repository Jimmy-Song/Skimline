# Skimline 圈选解释底部抽屉：对抗性审查

审查日期：2026-07-29

## 审查结论

底部抽屉、后台解释任务、跨标签恢复、三轮追问和无字幕降级已经形成完整闭环。审查中发现的高风险竞态和交互缺口均已修复，并补充了自动化回归测试。

## 审查范围

- 圈选菜单到抽屉打开、收起和再次圈选
- 抽屉独立滚动、固定输入区、历史问答折叠
- 标签页切换、视频切换、Side Panel 关闭和 Service Worker 重启
- 任务创建、去重、排队、取消、恢复、追问和三轮上限
- 字幕超时或缺失时的通用解释降级
- 模型请求超时、失败、忽略取消以及迟到返回
- 键盘退出、焦点返回、遮罩点击和无常驻入口
- 发布包可解压性与包内源码一致性

## 已发现并修复的问题

### 1. 旧浮卡定位函数残留

风险：窗口滚动或缩放时会调用已删除的 `positionExplanationCard`，产生运行时异常。

修复：移除旧定位调用；底部抽屉只依赖固定定位和自身状态。

### 2. 收起发生在任务 ID 返回之前

风险：用户点击“解释”后立即收起，字幕读取完成后才创建任务。旧实现拿不到任务 ID，无法标记主动收起，用户回到视频时抽屉可能意外重开。

修复：保留当前请求的主动收起状态；任务创建返回后立即发送 `DISMISS_CONTEXT_EXPLANATION`，不重新绑定 UI。

### 3. 快速重选造成旧请求反向覆盖新请求

风险：旧选区字幕读取较慢，新选区先开始后，旧请求仍可能晚到并取消或覆盖新任务。

修复：字幕完成和任务返回两个边界都检查 `explanationRequestId`；被替代的迟到任务会明确取消。

### 4. 模型忽略 Abort 后迟到写回

风险：任务已因语言切换或重选而取消，但底层请求忽略 Abort 并返回结果，状态可能从 `cancelled` 反转为 `complete`。

修复：模型返回后、落库前再次检查控制器和任务状态；迟到结果被丢弃。新增专项回归测试。

### 5. 追问失败覆盖首轮解释

风险：某一轮追问失败时，整个抽屉会替换成全屏错误，用户看不到已经成功的首轮解释和历史。

修复：有首轮结果时保留已有内容，只提示本轮失败并允许重新提问；只有首轮解释失败才显示完整重试态。

### 6. 收起动画期间内容瞬间清空

风险：抽屉尚未滑出就清空 DOM，用户会看到空白抽屉退出。

修复：状态立即清理，但 DOM 延迟到 260ms 退出动画结束后再释放；若期间重新打开则取消清理。

### 7. 焦点停留在 inert 抽屉

风险：键盘用户收起后焦点仍在已设为 `inert` 的抽屉内。

修复：优先把焦点还给来源观点按钮；来源 DOM 已变化时回到摘要主区域。

### 8. “回到最新回答”按钮可能压住输入区

风险：输入区存在或达到三轮后消失时，固定的 `bottom` 值不能同时适配两种布局。

修复：默认贴近内容底部；仅在输入区非空时通过 `:has()` 抬升到输入区上方。

### 9. 无字幕结果伪装成视频观点

风险：字幕不可用时模型仍可能生成 `inVideo` 和时间依据，让通用知识看起来像讲者原话。

修复：后台强制清空 `inVideo` 和 `evidence`，设置 `uncertain`，并显示“无法确认视频语境”的固定说明。

## 自动化验证

- `node --check background.js`
- `node --check sidepanel.js`
- 任务化解释专项：
  - 收起后继续运行并恢复
  - 无字幕安全降级
  - Service Worker 重启恢复
  - 三轮追问上限与完整历史
  - 取消后迟到结果不得反转状态
- 完整 `npm test`
- `npm run build:release`
- 发布 ZIP 解压测试与逐文件一致性校验

## 已知验证边界

Codex 内置浏览器的安全策略禁止自动导航到本地 `file://` 测试页，因此本轮未通过该自动化入口录制新的视觉截图。抽屉的 DOM、CSS、事件协议和本地 harness 已同步更新并由结构测试覆盖；最终在 Chrome 扩展侧加载发布包后，仍建议人工快速确认一次真实 Side Panel 的动画手感和不同侧栏宽度下的视觉密度。
