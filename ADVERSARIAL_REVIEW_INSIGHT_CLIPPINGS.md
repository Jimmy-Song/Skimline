# 圈选收藏功能对抗性审查记录

审查对象：Skimline 0.3.0 圈选收藏与洞见库
审查方式：在全量测试通过后，独立从竞态、失效状态、恶意数据、权限和边界交互重新检查实现。

## 审查覆盖

| 攻击面 | 检查内容 | 结果 |
|---|---|---|
| 并发写入 | 20 个收藏同时到达，是否丢失或覆盖 | 后台串行队列保证全部保留 |
| 写入失败 | 一次 `storage.local.set` 失败后，队列是否永久失效 | 已验证后续写入可以恢复 |
| 重复操作 | 连续点击、多个窗口保存同一段 | 后台按视频、时间和文本去重 |
| 陈旧响应 | 旧的列表读取是否覆盖刚保存或删除的新状态 | 使用 `revision` 拒绝旧响应 |
| 视频切换 | 保存请求进行中切换视频 | 保存点击时的快照，不读取新视频状态 |
| 概览时间 | 非法输入是否给概览伪造时间戳 | 后台强制 `overview.anchorT = null` |
| 非 YouTube 页面 | 打开来源是否覆盖用户当前的非视频页面 | 改为新建 YouTube 标签页 |
| 来源跳转 | 观点、0 秒观点和概览三种时间 | 分别生成正确 URL，概览不附加 `t` |
| 删除恢复 | 删除、跨窗口恢复、重复恢复 | 删除返回快照；恢复再次去重 |
| 容量上限 | 超过 1000 条是否静默删除旧数据 | 拒绝新收藏，不自动覆盖 |
| 存储脏数据 | 非法 ID、文本、时间和字段类型 | 读取和写入两端规范化 |
| 内容注入 | 收藏文字或标题包含 HTML | 全部使用 `textContent`，没有 HTML 注入入口 |
| 网络与隐私 | 收藏是否触发字幕读取、模型请求或新权限 | 不触发网络；权限列表保持不变 |
| 首次提示 | 摘要在洞见库后台完成时是否永久错过提示 | 返回摘要视图后再展示并持久化 |
| Toast 回调 | 撤销操作同步抛错是否成为未捕获异常 | 通过 Promise 链统一捕获 |
| 旧摘要缓存 | 收藏功能是否改变摘要缓存键或生命周期 | 存储键完全独立 |
| 文字缩放 | 新界面是否破坏全局 85%–125% 缩放 | 全部字体继续使用 `rem` |
| 发布包 | 新运行时文件是否进入 ZIP | 构建清单与发布包测试已覆盖 |

## 审查中发现并修复的问题

### 1. 陈旧列表覆盖新收藏

场景：Side Panel 启动时请求旧列表，用户立刻收藏；旧列表响应晚于保存响应。

修复：所有写入增加 `revision`；Side Panel 拒绝低于当前 revision 的列表响应。

### 2. 撤销回调同步异常没有被捕获

场景：Toast 的操作回调在返回 Promise 前同步抛错。

修复：改为 `Promise.resolve().then(onAction).catch(...)`。

### 3. 首次提示在后台完成时永久丢失

场景：用户正在洞见库中，视频摘要在隐藏视图完成，提示状态却提前标记为已展示。

修复：只有摘要视图真正可见时才展示并持久化；返回摘要视图时补触发。

### 4. 非法概览数据携带时间戳

场景：损坏或伪造的存储对象把概览收藏标记到具体时间。

修复：数据层根据 `sourceType` 强制清除概览时间戳。

### 5. 来源跳转覆盖非 YouTube 页面

场景：用户在普通网页打开洞见库并点击来源。

修复：仅在当前标签已经是 YouTube 视频时复用；否则新建来源标签页。

### 6. 安装文档仍指向旧版本目录

修复：下载包和解压目录统一更新为 0.3.0。

## 新增的防回归验证

- 收藏纯函数测试：9 项。
- 后台收藏生命周期、并发、非法输入和存储恢复测试：4 项。
- Side Panel 结构、隐私、陈旧响应和回调保护断言。
- 全项目自动化测试。
- 发布 ZIP 内容逐文件校验。

## 审查结论

实现覆盖开发方案的第一版范围，没有引入新的 Chrome 权限或收藏网络请求。对抗性审查发现的竞态和边界问题均已修复并纳入防回归检查。
