{
  "name": "youtube-viewpoint-map",
  "version": "0.5.1",
  "private": true,
  "description": "Skimline — a scannable, jump-anywhere map for long YouTube videos",
  "scripts": {
    "build:release": "sh scripts/build-release.sh",
    "build:cws": "sh scripts/build-release.sh --cws-only",
    "test": "node --test test/*.test.js"
  }
}
