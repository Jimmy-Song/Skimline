# Skimline 版本更新

从 Skimline 0.4.0 起，本文件按版本记录用户可感知的新增需求与重要修复。后续每次发布都在顶部追加独立版本章节。

## Skimline 0.5.1 — 2026-08-12

### 新增需求

- GitHub 手动安装包开始使用固定内层目录，并为固定扩展 ID 迁移提供逐设备备份、禁用、导入、核对和回退说明。
- 发布流程同时生成 GitHub 安装包与 Chrome Web Store 上传包；商店包使用根目录 manifest，并自动移除仅供开发版固定 ID 使用的公开 `key` 字段。
- 扩展启动时把 `chrome.storage.local` 限制为受信任扩展上下文访问，继续在本机保存 API Key、洞见库和摘要缓存，但不再向内容脚本暴露存储接口。

### 重要修复

- 避免版本号内层目录导致用户把新版加载成另一份扩展，并明确区分“禁用会保留数据”和“移除会清除本地数据”。
- 发布包改为严格文件白名单，防止 `.pem` 私钥、本机配置、开发文件或历史遗留文件进入 GitHub/CWS 产物。

## Skimline 0.5.0 — 2026-08-12

### 新增需求

- “搜索或提问”支持直接生成单次答卷：自动区分关键词检索、完整问答和歧义输入，先使用摘要快速回答，并可按需读取字幕升级答案；答卷支持两轮追问、失败重试、恢复、收藏和洞见库搜索。
- 洞见库的视频分组标题会随当前摘要语言本地化，同时保留原标题作为出处；历史缓存可惰性回填，本地化标题随收藏备份跨设备保存，并兼容旧版 Schema V2 备份。
- 流式生成观点期间，已聚焦卡片不再因列表重排而失去焦点，用户可以持续使用上、下方向键浏览正在生成的观点。

### 重要修复

- 修复字幕升级、追问和旧任务迟到响应之间的竞争条件，避免旧答案覆盖新问题或污染首答缓存。
- 修复洞见库标题回填覆盖新摘要、并发元数据写入互相覆盖，以及语言切换后标题未立即更新的问题。
- 修复流式观点合并时重复搬动已有 DOM 行，导致焦点回落到页面并使方向键导航中断的问题。

## Skimline 0.4.3 — 2026-08-10

### 新增需求

- “跟随播放”现在默认开启；首次加载和流式生成观点时，会自动展开当前播放观点，同时避免首次渲染抢占用户的滚动位置。
- 划词后的“收藏 / 解释 / 复制”操作浮层会根据侧栏可用空间自动调整宽度、位置和箭头偏移，贴近左右边缘时仍可完整操作。
- 洞见库入口改用单路径 SVG 书签图标，在不同缩放比例、主题和交互状态下保持完整清晰的描边。

### 重要修复

- 修复首次生成观点时，自动跟随逻辑可能引发滚动跳动的问题。
- 修复划词操作浮层靠近视口边缘时发生溢出、错位或箭头未指向选区的问题。
- 修复洞见库书签图标由 CSS 拼接导致部分描边缺失的问题。

## Skimline 0.4.2 — 2026-08-08

### 新增需求

- “搜索或提问”现在自动分流两类需求：关键词先在摘要中本地检索，完整返回所有直接命中的观点；没有字面命中时，才回退到 AI 精选最多 4 条语义相关观点。
- 本地结果支持主动“查找相关观点”：AI 只补充去重后的新观点，不替换已有结果，并明确显示本地命中数与 AI 新增数。
- 本地检索首次定位到信息最集中的观点，随后按视频时间使用上、下按钮浏览；关键词只在详情中出现时，会自动展开当前详情。
- 英文与数字关键词使用清晰的词边界，中文和符号查询保持字面匹配；搜索支持大小写、全角字符、多词 AND 和不限数量的结果。

### 重要修复

- 修复关键词查询被当成 AI 推荐、无论实际命中多少都最多只显示 4 条的问题。
- AI 补充返回后保持当前焦点、阅读位置和搜索前章节状态；清除、换查询或切换视频时，迟到响应不会污染当前结果。

## Skimline 0.4.1 — 2026-08-05

### 新增需求

- 摘要观点列表支持使用上、下方向键连续阅读：自动展开目标观点、跨章节打开对应分区，并将焦点和滚动位置同步到新观点。
- 切换 YouTube 标签页后，可恢复各标签页独立的跟随播放状态或手动阅读位置。

### 重要修复

- YouTube 中插广告播放期间不再用广告时间覆盖正片进度，避免观点地图错误跳回开头。
- 标签页快速切换时拒绝过期响应和旧跳转请求，避免其他视频的状态污染当前侧栏。

## Skimline 0.4.0 — 2026-08-04

### 新增需求

- 收藏持久层升级到 Schema V2，支持删除墓碑、多副本安全合并、v1 数据迁移以及完整 JSON 导入与导出。
- 洞见库支持按视频分组、折叠浏览、最近收藏排序、历史标题搜索和组内匹配。
- 收藏数据不再受 1000 条持久化上限影响；搜索覆盖全部数据，界面最多渲染最近或匹配的 1000 条收藏。
